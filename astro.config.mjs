import { defineConfig } from 'astro/config';

// GitHub Pages project site. If a custom domain is added later, set base to '/'.
export default defineConfig({
  site: 'https://alamparelli.github.io',
  base: '/5minute-learning',
});
