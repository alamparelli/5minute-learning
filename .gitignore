# build output
dist/
# dependencies
node_modules/
# generated types
.astro/
# loop memory (private backlog, never shipped, never published)
loop/
# misc
.DS_Store
*.log
.env
.env.production
# design explorations (kept local, not part of the product)
design/
